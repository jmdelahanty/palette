# Palette Zarr Schema and Writer Census

Status: generated review baseline; contradictions intentionally unresolved

This report is generated by `fisheye.diagnostics.zarr_storage_census`. It is a read-only static census: a record marked `unclassified`, `unclear`, or `candidate` is a prompt for review, not an inferred decision.

## Ground Truth Surfaces

- `450` `ArraySpec` occurrences, `270` leaf names, and `343` `(name, dtype, shape)` signatures;
- `43` `ArraySpec` leaf names with multiple signatures;
- `106` additional reader/validator required-array declarations, including training validation schemas;
- `1641` writer sites: `657` direct APIs, `983` resolved wrapper calls, and `1` manual metadata-created arrays;
- `348` writer sites retain a dynamic or unresolved array name;
- `508` writer-observed leaf names have no `ArraySpec` declaration.

The two JSON artifacts retain every occurrence and writer site. Repeated rows are intentional because a shared helper and each concrete caller are different migration and benchmark surfaces.

## What `ArraySpec` Represents Today

`ArraySpec` is existing logical validation evidence, not a complete storage contract. It carries a leaf name, free-form dtype string, symbolic shape template, required flag, and description. `StageSpec` can bind those fields to one stage path and fixed subgroups. Runtime validation checks presence, rank/dimensions, and broad dtype kind.

It does not identify a canonical schema ID/version, exact cross-language dtype, named axes, units, fill/null semantics, physical chunks/shards/codecs, lifecycle, consumers, or the writer that creates the array. Compatibility dtype strings such as `float16/float32/uint8` are intentionally non-exact. Standalone analysis tuple declarations also lack a concrete archive path. For those reasons this census never treats an `ArraySpec` occurrence as proof that an array is currently written.

## First Review Queue

- `144` declarations are standalone tuples without a `StageSpec` path binding.
- `348` writer sites still have a dynamic/unresolved array name.
- `1003` writer sites retain neither an inferred exact dtype nor an explicit dtype expression; `162` retain no static shape evidence.
- Only `489` writer sites can currently be assigned to a stage from a literal family path or stage-specific module.
- Raw physical arguments are visible at `727` chunk sites, `17` shard sites, and `15` codec sites. These are call-site counts, not distinct arrays or effective runtime layouts.
- The initial seven-contract catalog produces only `20` exact occurrence mappings; this is expected until contradictions are reviewed and more contracts are accepted.

## Writer Surface Counts

| Surface | Writer sites |
| --- | --- |
| benchmark | 25 |
| compaction | 24 |
| diagnostic | 14 |
| legacy | 49 |
| migration_or_maintenance | 47 |
| production | 1230 |
| publication | 72 |
| training | 180 |

## Declaration-to-Writer Coverage

- Declared leaf names: `270`
- Writer-observed leaf names: `630`
- Names present in both views: `122`
- Declared names without a statically resolved writer name: `148`
- Writer names absent from `ArraySpec`: `508`

Dynamic helpers mean the first gap list contains false gaps until a human binds caller paths. Conversely, writer-only names are exactly the surface that a declaration-only migration would miss.

## Ambiguous `ArraySpec` Leaf Names

Each row is deliberately unresolved. The next design pass should label it as same semantics, different semantics, or legacy variation.

| Leaf name | Signatures | Observed dtype/shape |
| --- | --- | --- |
| alignment_cos | 2 | float32 ['n_fish', 'n_bins']; float32 ['n_fish', 'n_step_frames'] |
| angular_velocity_deg_s | 2 | float32 ['n_fish', 'n_step_frames']; float32 ['n_samples'] |
| area_px | 4 | float32 ['n_rois', 'n_channels']; float32 ['n_rois']; float32 ['n_rows']; int32 ['n_rois'] |
| bbox_img_xyxy | 3 | float32 ['n_rois', 4]; float64 ['n_instances', 4]; float64 ['n_source_detections', 4] |
| bbox_norm_coords | 5 | float32 ['n_detections', 4]; float32 ['n_refined', 4]; float32 ['n_rois', 4]; float64 ['n_instances', 4]; float64 ['n_source_detections', 4] |
| bbox_valid | 2 | bool ['n_rois', 'n_channels']; bool ['n_rows'] |
| bbox_xyxy | 3 | float32 ['n_rois', 'n_channels', 4]; float32 ['n_rows', 4]; int32 ['n_rois', 4] |
| centroid_valid | 2 | bool ['n_rois', 'n_channels']; bool ['n_rows'] |
| centroid_xy | 2 | float32 ['n_rois', 'n_channels', 2]; float32 ['n_rows', 2] |
| class_ids | 4 | int32 ['n_detections']; int32 ['n_instances']; int32 ['n_refined']; int32 ['n_source_detections'] |
| confidence | 2 | float32 ['n_detections']; float64 ['n_rois'] |
| confidence_scores | 2 | float32 ['n_instances']; float32 ['n_source_detections'] |
| detection_source | 4 | int8 ['n_fish', 'n_step_frames']; int8 ['n_refined']; int8 ['n_rois']; int8 ['n_samples'] |
| distance_to_center_mm | 2 | float32 ['n_fish', 'n_bins']; float32 ['n_fish', 'n_step_frames'] |
| duration_s | 2 | float32 ['n_bouts']; float32 ['n_trials'] |
| edit_applied | 3 | bool ['n_rois', 'n_channels']; bool ['n_rois', 2]; bool ['n_rois'] |
| ellipse_params | 2 | float32 ['n_rois', 2, 5]; float32 ['n_rois', 5] |
| ellipse_success | 2 | bool ['n_rois', 2]; bool ['n_rois'] |
| end_frame | 2 | int32/int64 ['n_bouts']; int64 ['n_bouts'] |
| failure_reason_bytes | 2 | uint8 ['n_bouts', 'width']; uint8 ['n_rows', 'width'] |
| fish_id | 2 | int32 ['n_bouts']; int32 ['n_fish'] |
| fraction_approaching | 2 | float32 ['n_fish', 'n_bins']; float32 ['n_fish'] |
| fraction_following | 2 | float32 ['n_fish', 'n_bins']; float32 ['n_fish'] |
| frame_indices | 10 | int32 ['n_detections']; int32 ['n_instances']; int32 ['n_refined']; int32 ['n_rois']; int32 ['n_rows']; int32 ['n_samples']; int32 ['n_source_detections']; int32/int64 ['n_rows']; int64 ['n_samples']; int64 ['n_step_frames'] |
| instance_key | 4 | uint64 ['n_detections']; uint64 ['n_instances']; uint64 ['n_rois']; uint64 ['n_source_detections'] |
| mask_present | 3 | bool ['n_rois', 'n_channels']; bool ['n_rois']; bool ['n_rows'] |
| mask_probs_roi | 2 | float16/float32/uint8 ['n_rois', 'n_channels', 'H', 'W']; float16/float32/uint8 ['n_rois', 2, 'H', 'W'] |
| masks_roi | 2 | uint8 ['n_rois', 'n_channels', 'H', 'W']; uint8 ['n_rois', 2, 'H', 'W'] |
| mean_speed_mm_s | 3 | float32 ['n_bouts']; float32 ['n_fish', 'n_time_points']; float32 ['n_fish'] |
| optomotor_gain | 2 | float32 ['n_fish', 'n_bins']; float32 ['n_fish'] |
| radial_speed_mm_s | 2 | float32 ['n_fish', 'n_bins']; float32 ['n_fish', 'n_step_frames'] |
| reason_bytes | 4 | uint8 ['n_instances', 'width']; uint8 ['n_refined', 'width']; uint8 ['n_rois', 'width']; uint8 ['n_source_detections', 'width'] |
| review_notes | 2 | string ['n_instances']; string ['n_source_detections'] |
| scores | 2 | float32 ['n_detections']; float32 ['n_refined'] |
| source_detect_row_index | 4 | int32 ['n_detections']; int32 ['n_instances']; int32 ['n_rois']; int32 ['n_source_detections'] |
| source_refined_row_ids | 3 | int64 ['n_detections']; int64 ['n_rois']; int64 ['n_rows'] |
| start_frame | 2 | int32/int64 ['n_bouts']; int64 ['n_bouts'] |
| tail_angle_deg | 2 | float32 ['n_rows', 'n_tail_angles']; float32 ['n_rows', 'n_tail_samples'] |
| tail_angle_rad | 2 | float32 ['n_rows', 'n_tail_angles']; float32 ['n_rows', 'n_tail_samples'] |
| time_seconds | 2 | float32 ['n_samples']; float32/float64 ['n_rows'] |
| track_arena_ids | 2 | int32 ['n_tracks']; int32/int64 ['n_tracks'] |
| track_ids | 2 | int32 ['n_detections']; int32/int64 ['n_tracks'] |
| valid | 3 | bool ['n_bouts']; bool ['n_fish', 'n_step_frames']; bool ['n_rows'] |

## Review Checklist

- [ ] Review the ambiguous leaf-name rows one at a time; do not assign contract IDs by leaf name alone.
- [ ] Bind standalone analysis tuple declarations to concrete path templates.
- [ ] Review every `writer_without_array_spec_leaf_names` entry and decide whether it needs a canonical logical contract, a stage-local binding, or an explicit derived-cache/legacy designation.
- [ ] Review dynamic writer sites and bind helper parameters to concrete callers.
- [ ] Confirm consumers and access patterns against Crimson and Palette call sites; current heuristic labels include their basis.
- [ ] Confirm editable, append-only, and immutable lifecycle per concrete path before applying `StoragePlan`.
- [ ] Require every current writer path to have a benchmark case before enforcing the shared array factory.

## Machine-Readable Artifacts

- `docs/diagnostics/zarr_array_schema_census.json` — declarations, writer-observed schemas, ambiguities, and coverage gaps.
- `docs/diagnostics/zarr_production_writer_census.json` — direct APIs, wrapper call sites, physical arguments, lifecycle/access/consumer classifications, and unresolved fields.
- `docs/diagnostics/zarr_detection_schema_inventory.json` — detection-owned variants, downstream lineage, conflicts, and dated physical observations.
- `docs/diagnostics/zarr_detection_schema_inventory.md` — human review view of the detection-family inventory.
