import:
  resolutions: both
  use_kvikio_zarr: true
  use_sharding: true
  chunk_size: 16
  chunks_per_shard: 2
  batch_size: 32
  downsampled:
    size: [640, 640]
    method: area
    chunk_size: 32
