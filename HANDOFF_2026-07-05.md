# Session handoff — 2026-07-05

For the next Claude/agent picking up Palette. Supersedes `HANDOFF_2026-07-04.md`
(deleted this session; recover from git if needed). This session: verified the 07-01
review remediation with a 5-agent read-only pass, dispatched and landed the
review-remediation wave (4 slices), landed the inventory-accessor verb port, and
recorded the TensorRT realtime context.

## Repo state
- Branch `sun`, local HEAD `8bfe960`, working tree clean.
- **Full non-GPU suite GREEN on that exact tip: 3,382 passed / 2 skipped** (87–100s wall
  with `-n 16`). This is the trusted baseline; recount after any merge.
- **`origin` is UNREACHABLE from this sandbox** (SSH publickey denied); cached origin
  refs are stale. **Local `sun` is ground truth**; the maintainer pushes from their own
  environment. Never push or merge `sun` from an agent; agent branches only.
- `sun` moves between turns (parallel agents merge). Re-check clean fast-forward before
  merging; rebase agent branches at checkpoints.
- One agent worktree in flight at session end: `/tmp/palette-web-runtime-read-helpers-20260705173621`
  (branch `agent/web-runtime-read-helpers-…`, part of the ongoing labeling/web.py
  extraction stream). Everything else pruned.

## The 07-01 review, verified state (5-agent verification pass, 2026-07-05)

Every remediation-ledger claim in `docs/diagnostics/codebase_review_2026-07-01.md`
checked out against code — no overclaiming found. Scoreboard after this session's wave:

| Finding | Status |
|---|---|
| 1 deprecated pipeline path | **CLOSED** — eye stage deleted, `core/pipeline.py` marked legacy |
| 2 SQLite concurrency | **CLOSED as reframed** — busy_timeout 30s + atomic ingest; WAL deliberately off (NFS); single-writer funnel still future |
| 3 provenance | **CLOSED** — Slice 2 (`c0e111b`) + Phase 5 default-epoch bump (`0087be4`) + **content hashes landed late this session** (merge `62a8e52`): `content_v1` fingerprints joined into `run_provenance.input_artifacts` for all five model-loading writers (incl. a census-found U-Net path), SAM3 runtime identity (`manifest_v1` + checkpoint hash when explicit), deployment-artifact verification helper. Non-gating by design; gate stays `git_sha`+`config_hash`. Making artifact hashes value-required is a possible future epoch bump |
| 4 suite red/ungated | **CLOSED** — CI collect gate + not-gpu suite; whether the check is *required* in branch protection is unverifiable from the sandbox (API 403) — maintainer to confirm |
| 5 stage graph ×3 | **CLOSED as scoped** — pipeline↔catalog drift frozen (`test_stage_catalog_drift.py`), launcher edges now frozen too via `KNOWN_LAUNCHER_DEPENDENCY_OVERRIDES` (wave Slice B took the sanctioned fallback, not full derivation). Deliberate remainders: the "INTENT: converge" edges + the orchestrator convergence decision |
| 6 silent-wrong-data | **CLOSED** — fail-loud/data-parity fixes landed; grayscale conventions are centralized with zero pixel change (`390d438`). Value convergence remains a separate maintainer/model-parity decision |
| 7 web.py monolith | **IN PROGRESS** — 47k → ~20.9k and still shrinking via the labeling-extraction stream |

Trajectory metrics: utils/ 165k→144.7k and falling; eye_mask refs 112→55 files;
`registry/db.py` was the lone regression (8,186→8,518) — now frozen by the size ratchet
(below). The review's deferred-import/`_private` counts could not be reproduced with its
methodology; treat those rows as unmeasured.

## What landed this session (all on `sun`, suite green at tip)

- **Review-remediation wave** (`agents_todo/brief_review_remediation_wave_2026-07-05.md`,
  committed `b18f8b1`; all four slices landed and were verified against the tree):
  - **A — forcing function:** `[tool.importlinter]` contracts in `pyproject.toml`
    (layering + severed-utils forbiddens) and `scripts/check_file_size_ratchet.py` with
    committed baseline (`file_size_ratchet_baseline.json`), both wired into
    `.github/workflows/ci.yml` with a `git diff --exit-code` guard on the baseline.
    Baseline auto-tightens on shrink (`cd908dd`, `68a8a69`).
  - **B — launcher stage graph:** launcher stages resolve `canonical_id`/spec from the
    catalog; edges frozen by `KNOWN_LAUNCHER_DEPENDENCY_OVERRIDES` in
    `test_stage_catalog_drift.py` (fallback path per brief). No stage-graph copy can
    silently drift anymore.
  - **C — provenance Phase 5:** writer census (`docs/…/provenance` census `eca4537`),
    writers instrumented (`3dd6107`), `require_runs_parent` defaults new parents to
    `COMPLETION_EPOCH_REQUIRE_PROVENANCE` (=2); merge `0087be4`.
  - **D — hygiene:** TensorRT trtexec regexes fixed + path fallback (`4ec5e9d`),
    `shared/__init__` exports corrected (`eb81dae`), empty `io/` removed (`207b166`),
    legacy-pipeline bare except now logs (`910b878`), root scratch artifacts removed
    (`f8eefbf`), README expanded (`41cfac3`). LICENSE deliberately NOT added — that is a
    maintainer/HHMI decision, still open.
- **Inventory accessor verb port** (last open queue brief; merged `8bfe960` after
  commander verification): `Recording.artifact_inventory()` accessor, read-only
  `palette artifacts <recording>` verb with the standard envelope, `fisheye.api`
  exports, utils wrapper DELETED with docs retargeted (grep-proof zero refs).
  **Resolution decision to know:** the shared `status`/`plan` resolver prefers the
  **active training zarr** when a recording has both training + analysis stores, so
  `palette artifacts` inventories that one; `next_hints` says pass an explicit zarr path
  for the sibling. If analysis-first semantics are ever wanted, this is the place.
- **Adjacent work by parallel agents (verified or observed):** utils Phase 2 finished
  through items 4–5 (`registry/model_resolution.py` exists; misfiled modules moved,
  borrowed privates promoted); FrameDomains design + impl slice 1 (`shared/frame_domains.py`,
  `Recording.frame_domains()`); steady `labeling: extract …` stream continuing web.py
  decomposition; webknossos bridge spike docs (`91e6240`, `4995f06`).
- **TensorRT context note** (`docs/diagnostics/tensorrt_export_realtime_context_2026-07-05.md`,
  commit `effda9e`) — READ THIS before touching anything ONNX/TensorRT.

## TensorRT — standing instruction (maintainer-stated 2026-07-05)

The ONNX/TensorRT export path (`training/export_onnx.py`, `onnx_to_tensorrt.py`,
`export_shared.py`, deployment-artifact registration) is **load-bearing: the maintainer
builds engines to run realtime, closed-loop behavior from their acquisition library.**
It is NOT abandoned offline-pipeline optimization — an assessment this session nearly
misclassified it as a deletion candidate. **Fix, don't fold.** The offline batch
pipeline deliberately stays PyTorch `.pt` + FP16; adopt TRT offline only if profiling
proves forward-bound AND GPU-hours constrain. Open realtime-path follow-ups: engine
content-hash provenance (same class as the SAM3 canary), `.pt`-vs-`.engine` parity
census before closed-loop promotion (see
`docs/diagnostics/realtime_offline_detection_comparison_design_2026-06-17.md`).

## Queue (next work, roughly prioritized)

Every brief through the 07-05 wave is landed, including the vectorized-resolver
brief. Next dispatch needs a fresh brief (FrameDomains hot-path consumer
migrations — see item 2). Other next slices still need briefs too:

1. ~~Provenance content hashes~~ — **LANDED late this session** (brief
   `agents_todo/brief_provenance_content_hashes.md`, merge `62a8e52`, suite green
   3,401/2). Review Finding 3 fully closed; the autonomous-executor provenance gate is
   cleared.
2. **FrameDomains consumer migrations (Slice C)** — checkpoints 1–4 merged
   (`e657742`, `2cbb2b8`, `9034ead`, checkpoint 4 = `agent/frame-domains-consumers-items-4-5`).
   Per-item status lives in
   `docs/diagnostics/frame_domains_consumer_census_2026-07-05.md` (items 1–4 done,
   5 partial). **All migratable non-hot-path consumers are now migrated.** What
   remains and why it's blocked:
   - Items 5-remainder, 6, 8 (`regenerate_training_crops_pynvvc.py`,
     `detect_keypoints_yolo.py`, refine/curation/tracking paths): **UNBLOCKED** —
     the vectorized resolver LANDED (brief
     `agents_todo/brief_frame_domains_vectorized_resolver.md`, merge of
     `agent/frame-domains-vectorized`, commit `9ee35e9`): `convert()` is now
     internally vectorized via lazy per-edge lookup caches (dense-arange /
     capped sparse-dense / searchsorted), public API and error semantics
     unchanged, 26–50× on 1e6-value benchmarks, legacy loop kept in tests as
     oracle. These consumer migrations are the next slice — needs a fresh brief
     (item 6 `detect_keypoints_yolo.py` is a WRITER: checkpoint after the first
     writer migration).
   - Item 7 (`shared/crop_image_source.py`): gated on real acquisition crop-video
     dropped-frame evidence per the design approval record — do not migrate on a
     brief alone.
   - Item 9 (producer/exporter stampers): writer phase, last, after read-side
     patterns are stable.
   Approved patterns for future consumers: direct `FrameDomains(root=...)` for
   root-holding diagnostics, `Recording.frame_domains()` where a `Recording` is in
   hand; on resolver error, fall back to the verbatim legacy computation (see
   `training_image_profile.py` for the sentinel/duplicate-fallback exemplar).
   Accepted benign delta from checkpoint 2: coverage-dashboard total-frames max()
   can gain a stored-count candidate on archives lacking `original_frame_indices`
   (can only correct an undercount).
3. ~~utils shim deletion slice~~ — **LANDED** (merge `af93b19`): Phase 2 re-export
   shims deleted, `utils/system.py` removed entirely, marimo/docs importers
   retargeted. Import-linter "Phase 2 utility wrappers" contract enforces it.
4. **Detect-review legacy pointer cleanup** — whatever remains beyond the landed
   retirement + promotion-authority slices (grep target: the two annotated migrations
   should be the only writers).
5. ~~Grayscale unification~~ (review 6c) — **LANDED** (merge `390d438`): grayscale
   conventions centralized, zero pixel change. Review Finding 6 closed.
6. **web.py decomposition** — continuing via the extraction stream; db.py/maintenance
   decomposition, orchestrator convergence decision, and an e2e test remain the
   structural tier (need design decisions, not just briefs).
7. **Apptainer container** — still deferred (`docs/apptainer_deployment_vision.md`).
8. **Autonomous executor + transfer watcher** — provenance Phase 5 landed; content
   hashes (item 1) is the remaining gate per the roadmap.
9. **webKnossos bridge** — spike prep docs landed this session; needs the campus
   annotation-export conversation (`docs/labeling_platform_build_vs_adopt.md`).
10. **LICENSE** — maintainer/HHMI decision; flagged, not an agent task.

## Operating notes (carried forward + this session's lessons)

- Co-author trailer: `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`.
- **Verify agent headline claims against the repo before trusting them.** This session
  it worked in both directions: the wave agents' claims all verified true, AND a wrong
  premise of mine (TensorRT = dead code) was caught by the maintainer. Verify-before-
  baking-in remains the load-bearing discipline.
- Full `-m "not gpu"` suite on the merged tip before trusting any merged state
  (`PYTHONPATH=src ~/miniconda3/envs/palette-py311/bin/python -m pytest tests -m "not gpu" -q -n 16`).
- Env: conda `palette-py311`; never uv, never a `.venv`. Sync code — asyncio is fragile
  in this sandbox.
- Worktree hygiene: merge the moment a slice is verified green, prune the worktree and
  branches immediately (done consistently this session; no branch tangles).
- Briefs pattern that worked well: one brief file, four file-disjoint slices, premise
  corrections injected at dispatch, explicit "verify cited line numbers — re-locate by
  content." Keep doing that.
- The maintainer runs parallel agents heavily and drives origin pushes themselves.

## Human note (handle with care)

The maintainer had a hard stretch around self-worth / career doubt on 2026-07-04
(neuroscience PhD student, solo, LLM-assisted; advisor flagged a science-vs-engineering
fit tension). They value unflinching directness — do NOT flatter — but the self-critical
spiral is shame, not assessment, and a trained human should help with it if it
resurfaces: be honest, don't confirm the worthlessness, don't argue it away, gently
point toward their program's counseling, then meet them back in the work — building
something real and watching it verify is steadying. See
`~/skills_position_and_path_2026-07.md`. This session was steady and productive.
