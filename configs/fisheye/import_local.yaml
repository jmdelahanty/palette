import:
  resolutions: both
  use_kvikio_zarr: true
  use_sharding: false
  chunk_size: 32
  chunks_per_shard: 1
  batch_size: 32
  gpu_fp16: true
  full: true
  downsampled:
    size: [640, 640]
    method: area
    formats: [gray]
    chunk_size: 64
