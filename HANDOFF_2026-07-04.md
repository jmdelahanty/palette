# Session handoff — 2026-07-04

For the next Claude/agent picking up Palette. Written because context filled mid-session.

## Repo state
- Branch `sun`, local HEAD `8a79cd3`, working tree clean, full suite green as of the last merges.
- **`origin` is UNREACHABLE from this sandbox** (SSH publickey denied) and the cached
  `origin/sun` ref is ~15+ commits stale. So "ahead of origin by N" readings are unreliable
  here — treat **local `sun` as ground truth**; the maintainer pushes from their own
  environment. Verify origin state on their side, not from the sandbox.
- `sun` moves between turns (parallel agents merge). Always re-check a branch is still a
  clean fast-forward before merging; rebase onto current `sun` at each agent checkpoint.

## What landed this session (all on `sun`, verified green)
- **API-semantics arc complete:** A (palette import decoupled from Textual launcher +
  installed-entrypoint CI smoke), B (crop envelope reports `failed` on empty/failed; plan
  hints replayable + shell-safe), C (fail-closed review approval for keypoint/detect/
  subject-mask), D (verbs are importable `verb(request)->Envelope`; CLI is a parse+render
  shell; `_resolve_dataset_and_stages` at `cli/palette.py:909`).
- **Recording accessor + RunResolution:** `shared/run_resolution.py` (`resolve_run` with
  AUTHORITATIVE/LATEST_COMPLETE/INVENTORY_LATEST/SOURCE_MATCH), `shared/recording.py`
  (`open_recording`), `fisheye.api` curated surface. Detect two-pointer reconciliation DONE
  end-to-end (raw + refined crop paths resolve via AUTHORITATIVE; census proved 0 recordings
  changed; `backfill_detect_review_status` no longer writes the bypass pointer).
- **Identity:** instance keys (content-derived BLAKE2b key minted at detect, copied verbatim,
  no arange fallback, verified-when-present with legacy fallback — converts positional-trust
  bugs to loud failures); S1 fix (eye-split reconciles keypoint↔mask `source_crop_row_ids`
  before splitting, fail-closed).
- **Enforcement:** stage-array validation now enforces 7 stages (`_ENFORCE_STAGE_ARRAY_VALIDATION_FOR`
  in `registry/stage_complete.py`); keypoints `n_keypoints` sizing fixed.
- **utils Phase 1 drift-fix:** `_iter_zarr`/`_utc_now`/`_write_json`/`_infer_zarr_use`
  migrated to canonical `shared/` homes (0 local defs remain); `infer_zarr_use` precedence
  is `zarr_purpose → zarr_use → filename → default` (verified against writers).
- **Docs cleanup (this turn):** commits `2f68666` (eye-mask landmine fixes — see below),
  `16b6bb1` (archive spent + consolidate families), `8a79cd3` (archive diagnostic snapshots +
  drop dead eye-mask contract links).

## IN FLIGHT / uncertain
- **Provenance enforcement Slice 2** — design doc APPROVED and committed on branch
  `agent/provenance-finalization-design` (`docs/provenance_finalization_enforcement_design.md`,
  with the tiers table + job-ID/system-metadata + `system.py` relocation convergence + cluster
  git-state caveat added). The **implementation briefing was written** (in the conversation) but
  I'm not certain the implementation agent was spawned — CHECK. Key design decisions locked:
  enforce at `mark_run_complete`; monotonic epoch `COMPLETION_EPOCH_REQUIRE_PROVENANCE=2`;
  value-required gate = `git_sha`+`config_hash` ONLY; move `build_run_provenance` +
  `get_git_info`/`get_gpu_info`/`get_environment_summary` to `shared/` (kills the worst layering
  edge); record dirty-tree honestly; new stores get epoch 2, existing grandfathered.
- Possible other agents in `/tmp/palette-*` worktrees — run `git worktree list`.

## Docs cleanup — state & loose ends
- Root cause of ALL real landmines: the eye-mask severance (`4a85e5d`) deleted code but left
  6 eye-mask contract/workflow docs `status: active` + eye-mask sections in the live operator
  guides. Fixed: archived the 6 (+ LR eye investigation) with banners → `docs/archive/`,
  excised the dead-command sections from `operator_guide/{training_data,tuning_and_masks}.md`
  (live eye-COMPONENT review preserved), removed dead links from 3 live contracts.
- **STANDING RULE established:** when a slice deletes a subsystem, archive its contract/workflow
  docs IN THE SAME PASS — no `status: active` orphans.
- **Known remaining loose ends (LOW priority, me+LLMs substrate → low harm):** live→archive
  dangling markdown links exist (web-labeling checklists linked from live web-labeling docs;
  todos linking archived reviews; `codebase_review_2026-07-01` supersedes-links to older
  reviews). MANY of these pre-date this session (the substrate already had extensive
  live→archive links). Not worth chasing wholesale; retarget only if a specific one bites.
- The 4-agent triage produced full manifests (a–f, g–o, p–z, diagnostics/operator_guide) —
  reconstructable from the conversation if a deeper doc pass is wanted. `palette_crimson_contract_sync_todo.md`
  describes a never-created `docs/palette-crimson/` mirror — likely abandoned (flag).
- **Honest note:** the bulk archive/consolidate (Batch 2/3) was arguably more churn than value
  for a me+LLMs substrate; the LANDMINE fixes (Batch 1) were the high-value part. Don't
  over-invest in further doc tidying unless a human audience appears.

## Queue (next work, roughly prioritized)
1. **Provenance Slice 2 implementation** (if not already running) — the autonomous-execution gate.
2. **utils Phase 2** — move ~8 misfiled libs to `shared/` (`system.py` = 75 upward edges, the
   worst), promote ~10 privates, then import-linter contract forbidding `fisheye.utils`.
   (`system.py` relocation partly forced by provenance Slice 2 — coordinate.)
3. **`detect_review_status_latest` writer-surface retirement** — the broader follow-up the crop
   reconciliation scoped (writers: detect_review_backend, accept_detect_review, set_detect_review_status,
   promotion/migration, batch propagation).
4. **Identity Rec 2/3 remainder & FrameDomains resolver** — the frame-domain bug class.
5. **Apptainer container** — DEFERRED, see `docs/apptainer_deployment_vision.md`; picks up after
   provenance + utils cleanup.
6. **Autonomous executor + transfer watcher** — needs provenance Slice 2 first.
7. **webKnossos bridge spike** — needs the campus annotation-export conversation
   (`docs/labeling_platform_build_vs_adopt.md`).

## Design docs to read (status:proposed/active, all live)
`docs/palette_cli_narrow_waist_design.md` (verb/envelope/accessor + concepts primer),
`run_resolution_semantics.md`, `identity_lineage_staleness_review.md`,
`instance_identity_and_tracking_review.md`, `interface_and_execution_strategy.md`
(autonomous execution is the north star), `provenance_enforcement_roadmap.md`,
`utils_reorganization_strategy.md`, `stage_catalog_reality_gaps.md`,
`diagnostics/codebase_review_2026-07-01.md` (the current review + remediation ledger).

## Operating notes for the next Claude
- Co-author trailer: use `Claude Fable 5`, not Opus (earlier commits used Opus by mistake).
- Verify headline claims against the repo before trusting agent reports — this caught several
  wrong premises this session (infer_zarr_use precedence, the enforcement-union test, the RedScare
  "safe IF"). "Verify against the data before baking in a semantic assumption" is the load-bearing
  discipline.
- Full `-m 'not gpu'` suite before trusting any merged state, not just focused files.
- Worktree hygiene: merge a slice the moment it's green + prune its worktree; don't let "done"
  branches strand (a 6-branch tangle happened this session from exactly that).
- The maintainer runs parallel agents heavily and drives merges/pushes themselves.

## Human note (handle with care)
The maintainer had a very hard stretch this session around self-worth / career doubt (they're a
neuroscience PhD student, solo, LLM-assisted, and their advisor flagged a science-vs-engineering
fit tension). They value unflinching directness — do NOT flatter — but the self-critical spiral is
a distortion (shame, not assessment), not a true verdict, and it's the kind of thing a trained
human should help with. If it resurfaces: be honest, don't confirm the worthlessness, don't try to
win it by argument, and gently point toward their program's counseling. Then meet them back in the
work — building something real and watching it verify is steadying for them. See
`~/skills_position_and_path_2026-07.md` (an honest position doc written for them).
